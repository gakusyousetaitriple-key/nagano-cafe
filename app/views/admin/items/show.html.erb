<h1 class="text-center my-4">商品詳細</h1>

<div class="container">
  <div class="row">
    <!-- 左側に商品画像 -->
    <div class="col-md-6">
      <% if @item.image.attached? %>
        <%= image_tag @item.image, alt: @item.name, class: "img-fluid rounded shadow-sm" %>
      <% else %>
        <p>画像が登録されていません</p>
      <% end %>
    </div>

    <!-- 右側に商品情報 -->
    <div class="col-md-6">
      <div class="product-info">
        <p><strong>商品名:</strong> <%= @item.name %></p>
        <p><strong>商品説明:</strong> <%= @item.introduction %></p>
        <p><strong>ジャンル:</strong> <%= @item.genre.name %></p>
        <p><strong>税込価格(税抜価格):</strong> <%= (@item.price * 1.1).floor %>円 
          <span class="text-muted"><%= @item.price %>円</span>
        </p>
        <p><strong>販売ステータス:</strong> <%= @item.is_active ? "販売中" : "販売停止中" %></p>

        <!-- 編集ボタン -->
        <div class="mt-3">
          <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-primary" %>
        </div>
      </div>
    </div>
  </div>
</div>
